#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <boost/algorithm/string.hpp>
using namespace std;


int main() {
        
    int a, b, c;
    
    // srand(time(0));
    // // int n_items = 1000;
    // // float mr = 0.01;
    // // int gtoex = n_items * mr;
    // // cout << n_items << " * " << mr << " = " << gtoex << "    " <<rand()%n_items << endl;
    // float r = static_cast <float> (rand()) / static_cast <float> (RAND_MAX);

    // for(int i=0; i<10; i++) {
    //  r = static_cast <float> (rand()) / static_cast <float> (RAND_MAX);
    //     if(r > 0.5)
    //         cout << r << "\t Yes" << endl;
    //     else cout << r << "\t No" << endl;
    // }







    // vector<string> s;
    // vector<int> e;    
    // 
    // s.push_back("a");
    // s.push_back("b");
    // s.push_back("c");
    // s.push_back("d");
    // s.push_back("e");
    // // e.push_back(1);
    // // e.push_back(2);
    // // e.push_back(3);
    // // e.push_back(4);
    // // e.push_back(5);
    // 
    // e.erase(e.begin() + 1); s.erase(s.begin() + 1);
    // 
    // cout << s.size() << " | " << e.size() << endl;
    // // for(int i=0; i<)


    // Population p;
    // 
    // // // int n = 10;         // 1000 < n < 2000
    // // // int w = 100;         // 10000 < w < 20000
    // // // int s = 100;         // 10000 < s < 20000
    // // // // Lab1 l1(n);
    // // // l1.generate(n, w, s, "data.csv");
    // // // l1.see();
    // // // // l1.read("data.csv");
    // // 
    // // vector<string> strs;
    // // strs.push_back("aa");
    // // strs.push_back("ac");
    // // strs.push_back("ad");
    // // vector<vector<string>> vstrs;
    // // vstrs.push_back(strs);
    // // vstrs.push_back(strs);
    // // // 
    // // // vector<vector<string>> vec;
    // // // vector<int> a = {0,1};
    // // // vec.push_back(a);  
    // // // vec.push_back({2,3,4,5});
    // // // vec.push_back(a); 
    // // // 
    // // // for(vector<int>& v : vec){
    // // //     for(int& i : v)
    // // //         cout << i << " ";
    // // //     cout << endl;
    // // // }
    // // //     
    // cout << vstrs[1][0]  << endl;
}
